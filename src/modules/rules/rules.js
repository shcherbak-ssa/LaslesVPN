'use strict';

/** imports */
import checkEmail from './check-email';
import checkPassword from './check-password';

/** rules object */
const rules = {
  checkEmail,
  checkPassword
};

/** export */
export default rules;